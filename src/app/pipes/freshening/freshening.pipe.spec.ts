import { FresheningsPipe } from './freshening.pipe';

describe('FresheningsPipe', () => {
  it('create an instance', () => {
    const pipe = new FresheningsPipe();
    expect(pipe).toBeTruthy();
  });
});
